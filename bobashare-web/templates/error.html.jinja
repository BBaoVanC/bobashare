{% extends "base.html.jinja" %}

{% block title %}{{ code.as_u16() }}{% if let Some(reason) = code.canonical_reason() %} {{ reason }}{% endif %}{% endblock %}

{% block main %}
    <div class="error-message">
        <p>{{ message|capitalize }}</p>
    </div>
{% endblock %}
